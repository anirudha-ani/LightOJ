#include <cstdio>
#include <iostream>
#include <cstring>
#include <string>

using namespace std;

int main()
{
    int T , n , m ,x , y, z;
    int input[105];
    char a;

    scanf("%d", &T);

    for(int i = 0 ; i < T ; i++)
    {
        scanf("%d %d", &n , &m);

        for(int j = 0 ; j < n ; j++)
        {
            scanf("%d", &input[j]);
        }
        for(int j = 0 ; j < m ; j++)
        {

        }
    }
    return 0;
}
